"""
AUTO-GENERATED by RNGD Agent
combo: gemm → sin → gemv → add
"""
import torch


class AgentModel(torch.nn.Module):
    def __init__(self, dim: int = 64):
        super().__init__()
        self.linear_0 = torch.nn.Linear(dim, dim, bias=False)
        self.weight_1 = torch.nn.Parameter(torch.randn(dim, dim))

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        x = self.linear_0(x)
        x = torch.sin(x)
        x = torch.mv(self.weight_1, x.squeeze(0)).unsqueeze(0)
        x = x + x
        return x


def get_model():
    return AgentModel()


def get_sample_inputs():
    return (torch.randn(1, 64),)
